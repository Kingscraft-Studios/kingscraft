#pragma once

namespace lve {
    class Attributes {
    public:
        inline static float PLAYER_WIDTH  = 0.6f;
        inline static float PLAYER_HEIGHT = 1.8f;
        inline static float EYE_HEIGHT    = 1.62f;
        inline static float WALK_SPEED    = 3.0f;
        inline static float GRAVITY       = -20.0f;
        inline static float TERMINAL_FALL_SPEED = 30.0f;
        inline static float JUMP_STRENGTH = 8.0f;
        inline static float STEP_HEIGHT   = 0.5f;
    };
}
